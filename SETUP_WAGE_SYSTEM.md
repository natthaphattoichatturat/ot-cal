# 📖 คู่มือการตั้งค่าระบบค่าจ้าง

## 🎯 สาเหตุที่หน้า LIFF และ /wages/[id] แสดง 0

ระบบต้องการ **`wage_summary` table** เพื่อเก็บข้อมูลค่าจ้างที่คำนวณแล้ว แต่ยังไม่มีในฐานข้อมูล!

---

## ⚙️ ขั้นตอนการตั้งค่า (ทำครั้งเดียว)

### 1. สร้าง Table ในฐานข้อมูล Supabase

1. เปิด [Supabase Dashboard](https://supabase.com/dashboard)
2. เข้าโปรเจค: `clmzzsyxrymhbfvyclwe`
3. คลิก **SQL Editor** ในเมนูด้านซ้าย
4. คัดลอกโค้ด SQL จากไฟล์ `wage_summary_migration.sql`
5. วางและกด **Run** เพื่อสร้างตาราง
6. ตรวจสอบว่าตารางถูกสร้างแล้วใน **Table Editor**

---

## 🚀 การใช้งานระบบ

### ขั้นตอนที่ 1: Import ข้อมูลการเข้างาน
1. ไปที่หน้าหลัก (`/`)
2. เลือกไฟล์ `.txt` จากเครื่องสแกน
3. กด "นำเข้าข้อมูล"
4. ระบบจะประมวลผลและบันทึกลง `daily_attendance`

### ขั้นตอนที่ 2: คำนวณค่าจ้าง ⭐ **ใหม่!**
1. ไปที่หน้า **"ค่าจ้าง"** (`/wages`)
2. เลือกเดือนและปีที่ต้องการ (เช่น พฤศจิกายน 2568)
3. กดปุ่ม **"🧮 คำนวณและบันทึกค่าจ้าง"**
4. รอสักครู่... ระบบจะ:
   - คำนวณค่าจ้างจาก `daily_attendance` ทั้ง 2 งวด
   - คำนวณ SSO, ภาษี, เบี้ยขยัน
   - บันทึกลง `wage_summary` table

### ขั้นตอนที่ 3: ตรวจสอบผลลัพธ์
หลังจากคำนวณเสร็จ:

#### ✅ หน้า Web `/wages/[id]`
- เลือกพนักงาน → จะเห็นข้อมูลครบถ้วน:
  - **รายได้งวดนี้**: ค่าจ้าง, OT, รวมรายได้
  - **📊 ยอดสะสมรายปี 2568**: 6 รายการ (เงินเดือน, ภาษี, SSO, รวมได้, รวมหัก, สุทธิ)
  - **🌟 ยอดสะสมทั้งหมด**: 6 รายการ (ตั้งแต่เริ่มงาน)

#### ✅ หน้า LIFF `/liff/employee-ot-viewer`
- พนักงานเปิด LINE OA → เลือกดูค่าจ้าง
- แสดงยอดสะสม 12 รายการเหมือนหน้า Web

---

## 📊 ข้อมูลที่แสดง (12 รายการ)

### ยอดสะสมรายปี (YTD) - 6 รายการ
1. เงินเดือนสะสมทั้งปี
2. ภาษีเงินได้สะสมทั้งปี
3. ประกันสังคมสะสมทั้งปี
4. รวมเงินได้สะสมทั้งปี
5. รวมหักสะสมทั้งปี
6. เงินได้สุทธิสะสมทั้งปี

### ยอดสะสมทั้งหมด (All-Time) - 6 รายการ
7. เงินเดือนสะสมทั้งหมด
8. ภาษีเงินได้สะสมทั้งหมด
9. ประกันสังคมสะสมทั้งหมด
10. รวมเงินได้สะสมทั้งหมด
11. รวมหักสะสมทั้งหมด
12. เงินได้สุทธิสะสมทั้งหมด

---

## 🎨 UI/UX ใหม่

### สีและธีม
- โทนหลัก: **ขาว-ฟ้า-น้ำเงินเข้ม-กรมท่า**
- ยอดสะสมรายปี (YTD): พื้นขาว + highlight สีน้ำเงิน
- ยอดสะสมทั้งหมด (All-Time): พื้นขาว + highlight สีกรมท่า
- เน้นความชัดเจน อ่านง่าย สบายตา

### การออกแบบ
- ใช้ grid responsive สำหรับแสดงข้อมูล
- แยกสีชัดเจนระหว่าง "รายได้" (สีน้ำเงิน) และ "รายหัก" (สีแดง)
- "เงินสุทธิ" โดดเด่นด้วย background เข้มและตัวหนา

---

## 🔄 Workflow สรุป

```
1. Import Attendance (/) 
   → daily_attendance table ✅
   
2. Calculate Wages (/wages) 
   → wage_summary table ✅
   
3. View Results:
   • Web: /wages/[id] → แสดงยอดสะสม 12 รายการ ✅
   • LIFF: /liff/employee-ot-viewer → แสดงยอดสะสม 12 รายการ ✅
```

---

## 📝 หมายเหตุสำคัญ

### ⚠️ ต้องคำนวณทุกครั้งที่
1. Import attendance ใหม่
2. แก้ไขข้อมูลการเข้างาน
3. เปลี่ยนเงินเดือนพนักงาน
4. ต้องการดูข้อมูลล่าสุด

### 💡 Tips
- คำนวณแยกทีละเดือนเพื่อความแม่นยำ
- ตรวจสอบข้อมูล `daily_attendance` ให้ถูกต้องก่อนคำนวณ
- ถ้ายังแสดง 0 ลองคำนวณใหม่อีกครั้ง

### 🐛 การแก้ปัญหา

**ปัญหา:** LIFF แสดง 0.00 ทั้งหมด  
**วิธีแก้:** 
1. ตรวจสอบว่ามีข้อมูลใน `daily_attendance` สำหรับเดือน/ปีที่เลือก
2. ไปที่ `/wages` → กดปุ่ม "คำนวณค่าจ้าง" สำหรับเดือนนั้น
3. Refresh หน้า LIFF

**ปัญหา:** `/wages/[id]` แสดงเฉพาะรายเดือน  
**วิธีแก้:**
1. ตรวจสอบว่ามี `wage_summary` table แล้ว
2. คำนวณค่าจ้างสำหรับเดือนนั้น
3. Refresh หน้า

---

## 📚 API Endpoints

### POST `/api/wages/calculate`
คำนวณและบันทึกค่าจ้างลง `wage_summary`

**Request Body:**
```json
{
  "month": 11,
  "year": 2025
}
```

**Response:**
```json
{
  "success": true,
  "message": "Calculated and saved 20 wage records",
  "calculated": 20
}
```

### GET `/api/employees/[id]/ytd`
ดึงยอดสะสมรายปี

**Response:**
```json
{
  "success": true,
  "data": {
    "ytd_gross_wage": 120000.00,
    "ytd_tax": 1200.00,
    "ytd_sso": 4500.00,
    "ytd_total_income": 150000.00,
    "ytd_total_deduction": 5700.00,
    "ytd_net_wage": 144300.00
  }
}
```

### GET `/api/employees/[id]/all-time-summary`
ดึงยอดสะสมทั้งหมด

---

## ✅ Checklist การทดสอบ

- [ ] สร้าง `wage_summary` table ใน Supabase
- [ ] Import attendance ตัวอย่าง
- [ ] คำนวณค่าจ้างผ่านปุ่มในหน้า `/wages`
- [ ] เปิดหน้า `/wages/[id]` → เห็นยอดสะสม 12 รายการ
- [ ] เปิด LIFF `/liff/employee-ot-viewer` → เห็นยอดสะสม 12 รายการ
- [ ] ตรวจสอบสีและธีมเป็นโทนขาว-ฟ้า-น้ำเงิน
- [ ] ทดสอบ deploy บน Vercel

---

**สร้างเมื่อ:** 20 พฤศจิกายน 2568  
**เวอร์ชัน:** 1.0.0  
**โดย:** AI Assistant

