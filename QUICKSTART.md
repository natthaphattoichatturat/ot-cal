# Quick Start Guide - ระบบคำนวณ OT

## เริ่มต้นใช้งานภายใน 5 นาที

### ขั้นตอนที่ 1: ตั้งค่า Database

1. เปิด [Supabase Dashboard](https://supabase.com/dashboard)
2. ไปที่โปรเจค: `clmzzsyxrymhbfvyclwe`
3. คลิก **SQL Editor** ในเมนูด้านซ้าย
4. คัดลอกเนื้อหาจากไฟล์ `data.sql` แล้ววางในหน้า SQL Editor
5. คลิก **Run** เพื่อสร้างตาราง
6. คัดลอกเนื้อหาจากไฟล์ `sample_employees.sql` และรันเช่นกัน เพื่อเพิ่มข้อมูลตัวอย่าง

### ขั้นตอนที่ 2: รันโปรเจค

```bash
# ติดตั้ง dependencies (ทำครั้งเดียว)
npm install

# รันโปรเจคในโหมด development
npm run dev
```

เปิดเบราว์เซอร์ที่: http://localhost:3000

### ขั้นตอนที่ 3: ทดสอบระบบ

1. **นำเข้าข้อมูลตัวอย่าง**
   - คลิกปุ่ม "เลือกไฟล์" บนหน้าหลัก
   - เลือกไฟล์ `sample_scan_data.txt`
   - คลิก "นำเข้าข้อมูล"
   - รอสักครู่ ระบบจะประมวลผลและแสดงผลในตาราง

2. **ดูข้อมูล OT**
   - เลือกเดือน: ตุลาคม
   - เลือกปี: 2568 (2025)
   - ระบบจะแสดงตารางข้อมูล OT แบ่งเป็น 2 งวด

3. **ทดสอบการลางาน**
   - คลิกปุ่ม "บันทึกการลา"
   - เลือกพนักงาน, วันที่, และประเภทการลา
   - คลิก "บันทึกการลา"
   - ดูประวัติการลาในตารางด้านล่าง

## โครงสร้างหน้าเว็บ

### หน้าหลัก (/)
- แสดงข้อมูลวันที่และเวลาปัจจุบัน
- ฟอร์มนำเข้าข้อมูลการสแกน
- เลือกเดือน/ปีที่ต้องการดู
- ตารางแสดง OT แบ่งเป็น 2 งวดบัญชี:
  - **งวดที่ 1**: วันที่ 26 (เดือนก่อน) - 10
  - **งวดที่ 2**: วันที่ 11 - 25
- แต่ละ cell แสดงชั่วโมง OT
- สีพื้นหลังบ่งบอกวันในสัปดาห์

### หน้าบันทึกการลา (/leave)
- ฟอร์มบันทึกการลางาน
- ค้นหาประวัติการลา
- ตารางแสดงประวัติการลาทั้งหมด

## รูปแบบไฟล์ Import

ไฟล์ .txt ต้องมีรูปแบบดังนี้:

```
machine_id[TAB]dd-mm-yyyy[TAB]HH:MM:SS[TAB]'employee_id[TAB]scan_type
```

ตัวอย่าง:
```
01	20-10-2025	07:45:00	'20056402	1
04	20-10-2025	17:15:30	'20056402	2
```

ที่ต้องจำ:
- คั่นด้วย TAB (ไม่ใช่ space)
- วันที่เป็น dd-mm-yyyy
- employee_id มี ' (single quote) นำหน้า
- scan_type: 1 = เข้างาน, 2 = ออกงาน

## กฎการคำนวณ OT (สรุป)

### กะที่ 1 (08:00-17:00)
- OT เช้า: 06:00-08:00 (สูงสุด 2 ชม.)
- OT ค่ำ: 17:30 เป็นต้นไป

### กะที่ 2 (20:00-05:00)
- OT เย็น: 17:30-20:00
- OT เช้า: 05:30 เป็นต้นไป

### อัตราคูณ
- **วันธรรมดา**: × 1.5
- **วันอาทิตย์/วันหยุด**:
  - 8 ชม.แรก × 2
  - เกิน 8 ชม. × 3

## ฟีเจอร์พิเศษ

### 1. ตรวจสอบข้อมูลซ้ำ
- ระบบจะไม่นำเข้าข้อมูลที่ซ้ำกัน
- แจ้งจำนวนข้อมูลที่นำเข้าสำเร็จและข้ามไป

### 2. คำนวณอัตโนมัติ
- คำนวณ OT ทันทีหลังนำเข้า
- อัปเดตข้อมูลในตารางแบบ Real-time

### 3. UI แบบ Excel
- ออกแบบให้คล้าย Microsoft Excel
- ใช้งานง่ายสำหรับผู้ที่คุ้นเคยกับ Excel

### 4. สีแบ่งวัน
- แต่ละวันมีสีต่างกัน (จันทร์-อาทิตย์)
- ช่วยมองเห็นวันหยุดและวันธรรมดาได้ชัดเจน

## การแก้ปัญหา

### ไม่แสดงข้อมูล OT
1. ตรวจสอบว่ามีข้อมูลพนักงานใน database หรือไม่
2. ตรวจสอบรูปแบบไฟล์ที่ import (ต้องเป็น TAB)
3. เช็ค Console (F12) เพื่อดู error

### Import ไม่สำเร็จ
1. ตรวจสอบรูปแบบไฟล์ให้ถูกต้อง
2. ตรวจสอบว่า employee_id มีในตาราง employees
3. ตรวจสอบ network connection กับ Supabase

### คำนวณ OT ไม่ถูกต้อง
1. ตรวจสอบข้อมูลการสแกนเข้า-ออกให้ครบ
2. ตรวจสอบวันหยุดในตาราง special_holidays
3. ตรวจสอบ logic ใน `lib/otCalculator.ts`

## ติดต่อ / รายงานปัญหา

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. เช็ค README.md สำหรับข้อมูลเพิ่มเติม
2. ดู Console logs (F12 > Console)
3. ตรวจสอบ Supabase logs

## สิ่งที่ควรรู้

### การทำงานข้ามวัน
- พนักงานกะ 1 สามารถทำงานข้ามวันได้
- พนักงานกะ 2 ปกติจะทำงานข้ามวันอยู่แล้ว
- ระบบจัดการเวลาข้ามวันอัตโนมัติ

### การบันทึกงวดบัญชี
- งวด 1: 26 (เดือนก่อน) - 10
- งวด 2: 11 - 25
- เป็นไปตามมาตรฐานการบัญชีของบริษัท

### ความปลอดภัยข้อมูล
- ข้อมูลทั้งหมดเก็บบน Supabase (PostgreSQL)
- มี Row Level Security (RLS)
- เข้าถึงผ่าน API Key ที่ปลอดภัย

---

**เริ่มต้นใช้งานได้เลยตอนนี้! 🚀**
